package accent.html.demo.routes.users._name;

import accent.html.Component;
import accent.html.Html;
import accent.html.demo.User;
import accent.html.demo.UserCard;

public record Page(User user) implements Component {

    @Override
    public Html render() {
        return (
            <UserCard user={this.user} />
        );
    }
}
