package io.lemadane.accent.html.demo.routes.users._name;

import io.lemadane.accent.html.Component;
import io.lemadane.accent.html.Html;
import io.lemadane.accent.html.demo.User;
import io.lemadane.accent.html.demo.UserCard;

public record Page(User user) implements Component {

    @Override
    public Html render() {
        return (
            <UserCard user={this.user} />
        );
    }
}
